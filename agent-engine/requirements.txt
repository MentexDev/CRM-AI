# POC motor agéntico · CrewAI
# Python 3.10–3.13
#
# Versiones con cota superior (evita saltos mayores no probados). Para máxima
# integridad de la cadena de suministro, genera un lock con hashes:
#   pip install pip-tools
#   pip-compile --generate-hashes requirements.txt -o requirements.lock.txt
# y en despliegue/CI instala desde el lock:
#   pip install --require-hashes -r requirements.lock.txt
crewai>=0.130,<1.0
python-dotenv>=1.0.0,<2.0
requests>=2.32.4,<3.0          # >=2.32.4 cierra CVE-2024-35195
# API web (despliegue)
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
