import { CsComing } from './CsShell'

export default function CsContacts() {
  return (
    <CsComing
      title="Contactos"
      subtitle="Clientes que escriben por los canales."
      what="Aquí va la lista de contactos (nombre, número, etiquetas, historial). Se crean automáticamente cuando un número escribe (Fase 2) y también a mano. Se construye en la próxima tanda."
    />
  )
}
