import { CsComing } from './CsShell'

export default function CsSettings() {
  return (
    <CsComing
      title="Configuración"
      subtitle="Etapas del pipeline y ajustes del workspace."
      what="Aquí editas las etapas del Kanban (crear, renombrar, reordenar, color) — ya vienen unas por defecto — y otros ajustes de atención. Se construye en la próxima tanda."
    />
  )
}
